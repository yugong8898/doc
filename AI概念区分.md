# Skill / Prompt / Agent / Model / Loop / Graph / LLM / RAG / Harness 概念区分

## 九个概念的关系

```
运行治理 (Harness)    → 约束框架：谁能触发、用何权限、证据存哪、如何暂停/恢复/回滚
  ↑ 治理
工作流图 (Graph)      → 蓝图，编排多个 Agent/Skill，定义"谁依赖谁、哪里并行、哪里汇合"
  ↑ 编排
Agent (智能体)        → 人，能读规则、用工具、调技能、自主完成多步任务
  ↑ 内部运行
Loop (执行循环)       → 心跳，反复「思考→行动→观察」，定义"失败回哪、怎样继续、何时收敛"
  ↑ 由…驱动
Model (模型)          → 抽象引擎层，定义"智能从哪来"
  ↑ 具体实现
LLM (大语言模型)      → 真实引擎，跑在背后的大模型（如 Qoder 背后的模型）
  ↑ 由…增强
RAG (检索增强)        → 外挂知识库，先把资料检索出来喂给 LLM，减少胡说
  ↑ 配备
Skill (技能)          → 背包里的工具，写好打包，用斜杠命令随时取用
  ↑ 简化版
Prompt (提示词)       → 便签纸，临时写的指令，用完就扔
```

## 逐个拆解

### 1. Model（模型）— 引擎

驱动 AI 的"引擎"这一**抽象层**——定义智能从哪来、怎么推理。它落地后的真实实例是 LLM（见第 7 节）。你不需要"使用"它，它是跑在背后的引擎。

- **你能做的**：在设置里选模型（如果支持，本质是在选不同的 LLM）
- **你改不了的**：模型的能力边界、推理方式
- **项目中的例子**：Qoder 背后跑的那个"引擎"就是 Model 层，具体落地为某个 LLM

### 2. Prompt（提示词）— 便签纸

临时写的一段指令，告诉 AI 这次要干什么。

- **特点**：一次性、临时性、每次手写
- **项目中的例子**：`.md/prompts/role-coder.md` 就是 prompt，在对话里 `@role-coder.md` 引用它
- **缺点**：每次都要手动引用，容易忘，格式不统一

### 3. Skill（技能）— 工具箱

把 prompt **打包升级**：加上名称、描述、触发条件，注册成可复用的技能，用斜杠命令激活。

| 对比 | Prompt | Skill |
|------|--------|-------|
| 激活方式 | 手动 `@文件路径` | `/skill-name` 一键调用 |
| 发现性 | 靠记忆，容易忘 | 系统列出可用技能 |
| 复用性 | 每次引用 | 写一次，到处调 |
| 结构 | 纯 markdown | markdown + frontmatter 元数据 |

- **项目中的例子**：`.qoder/skills/commit-message/SKILL.md` 就是 Skill，输入 `/commit-message` 就能用
- **适合场景**：反复做的标准流程（提交信息、代码审查、需求分析）

### 4. Agent（智能体）— 执行者

能**自主决策**的实体，可以读规则、用工具、调技能、多步推理完成复杂任务。

- **主 Agent**：对话中的 AI 助手，能读 `.cursorrules`、能搜索代码、能编辑文件
- **Sub Agent**：专用子智能体，比如 Qoder 内置的 `CodeReview`（代码审查）、`Debug`（调试）
- **项目中的例子**：你说"帮我改这个需求"，Agent 会自动读规则 → 搜索代码 → 分析影响 → 写代码 → 自检，这一串是 Agent 自主完成的

### 5. Loop（执行循环）— 心跳

Agent 的**运行时机制**：不是"一次想完就结束"，而是反复「思考 → 行动 → 观察结果 → 再思考」循环迭代。它要回答三个核心问题：**失败后回到哪里、怎样继续、什么时候算收敛**。

- **本质**：把一次性的"问答"变成可自我纠错的"过程"；收敛条件（成功 / 到达上限 / 无新进展）决定何时停下
- **触发方式**：Agent 内部自动跑，无需手动开启；可设最大轮次（如 sub agent 的 `max_turns`）
- **项目中的例子**：`wlyd-workflow` 把需求 → 分析 → 方案 → 编码 → 审查 → 提测串成流水线，某阶段自检不通过就**回到上一轮重做**（失败后回到哪）、按反馈继续推进（怎样继续），直到各阶段全部通过才算**收敛**；Qoder 的 `CodeReview` / `Debug` 子智能体也是循环执行直到收敛

### 6. Graph（工作流图）— 蓝图

把多个 **Agent / Skill / Prompt** 编排成一张**有向图**。它要回答三个核心问题：**谁依赖谁、哪里可以并行、哪里必须汇合**。

- **本质**：在 Agent（单兵）之上的一层**编排**，解决"多步骤、多角色如何协同"
- **特点**：依赖边决定先后、汇合点决定同步；节点结构支持带状态（为断点续传提供基础）
- **项目中的例子**：`wlyd-workflow` 本身就是流程图编排器，把 `role-pm` / `role-architect` / `role-coder` / `role-reviewer` 这些角色（Prompt）和审查 / 提测（Skill）编排成有向图，显式定义阶段间的依赖与汇合；业界如 LangGraph 也是典型 Graph 框架

### 7. LLM（大语言模型）— 真实引擎

Model 是"抽象引擎层"（定义智能从哪来），LLM 是它落地后的**真实实例**——那个具体的大模型（GPT、Claude、Qoder / Cursor 背后的模型等）。

- **你能做的**：在设置里选哪个 LLM（如果支持切换）
- **你改不了的**：模型参数、训练数据、推理方式
- **项目中的例子**：Qoder 背后跑的大模型、Cursor 背后的模型，就是 LLM；Agent 每轮"思考"生成的文字都来自它

### 8. RAG（检索增强生成）— 外挂知识

Retrieval-Augmented Generation：给 LLM **外挂一个知识库**，先检索相关资料，再把资料连同问题一起喂给 LLM，让它基于"有据可查"的内容回答，减少幻觉（胡说）。

- **本质**：用"检索"补 LLM 的"记忆有限 / 知识过期"
- **触发方式**：Agent 在回答前自动检索（或你手动触发检索工具），把结果注入上下文
- **项目中的例子**：本项目的 `RAG_search` 工具（检索知识库）、`.repoWiki/` 知识库被检索后注入上下文，本质上就是 RAG；写码时把已知坑点（known-pitfalls）检索出来喂给模型，也属于 RAG 思路

### 9. Harness（运行治理）— 约束框架

包裹整个执行过程的**运行时与治理层**。它不决定"做什么"，而决定"在什么约束下做、做完留下什么痕迹、出错怎么收拾"。它要回答四个核心问题：**谁能触发、能用什么权限、证据放在哪里、怎么暂停、恢复和回滚**。

- **本质**：Agent / Loop / Graph 的"操作系统"——提供权限沙箱、证据（日志 / 产物）落盘、生命周期控制
- **关注点**：触发者身份、权限边界、证据留存位置、暂停（pause）/ 恢复（resume）/ 回滚（rollback）机制
- **项目中的例子**：CodeBuddy 的 Agent 运行环境就是 Harness——它决定你能派 sub agent、能读写哪些文件、把执行证据写进对话与日志，并支持断点续传（`wlyd-workflow` 的"断点续传"本质就是 Harness 层的状态暂停与恢复）；git 的版本回退则是典型的 rollback 能力

## 一句话记忆法

| 概念 | 一句话 | 项目中的例子 |
|------|--------|--------------|
| **Model** | 跑在背后的引擎，不用管 | Qoder 的大模型 |
| **Prompt** | 临时便签，用完即弃 | `@role-coder.md` |
| **Skill** | 打包好的工具，`/命令` 调用 | `/commit-message` |
| **Agent** | 干活的人，能读规则用工具 | 对话中的 AI 助手 |
| **Loop** | Agent 的心跳，反复试错直到完成 | `wlyd-workflow` 各阶段自检回环 |
| **Graph** | 把多兵种编排成流程图的蓝图 | `wlyd-workflow` 研发流程编排 |
| **LLM** | 真实引擎，每轮推理的智能来源 | Qoder / Cursor 背后的大模型 |
| **RAG** | 外挂知识库，让 LLM 有据可查 | `RAG_search` + `.repoWiki/` |
| **Harness** | 运行治理，管权限/证据/暂停恢复回滚 | CodeBuddy Agent 运行环境 |

## 项目中怎么搭配用

```
Agent（AI 助手）
├── 自动读取 → .cursorrules / .qoder/rules/（项目规则，每次生效）
├── 手动引用 → .md/prompts/role-xxx.md（Prompt，按需引用）
├── 斜杠命令 → .qoder/skills/xxx（Skill，按需触发）
├── 派子智能体 → CodeReview / Debug（Sub Agent，专用任务）
├── 循环执行 → 每个 Agent 内部靠 Loop 反复「思考→行动→观察」直到收敛
├── 流程编排 → Graph（如 wlyd-workflow）把多 Agent / Skill / Prompt 串成有向图，决定先后与回退
├── 底层引擎 → LLM（大模型）为所有推理提供智能，选哪个由设置决定
├── 检索增强 → RAG 把知识库检索结果喂给 LLM，让它"有据可查"
└── 运行治理 → Harness 管触发权限、证据落盘、暂停/恢复/回滚，约束整个执行
```

## 最佳实践

- **规则类**（每次都要遵守的）→ 放 `.cursorrules` / `.qoder/rules/`
- **流程类**（反复执行的标准步骤）→ 转成 Skill
- **临时类**（偶尔用一次的）→ 直接 Prompt 就行，不用转 Skill
- **复杂类**（需要多步自主完成的）→ 交给 Agent，必要时派 Sub Agent
- **迭代类**（单 Agent 内部需反复试错 / 自检）→ 交给 Loop 自动跑，设好最大轮次
- **协同类**（多个 Agent / 角色要按序或并行协作）→ 用 Graph 编排，别手写线性脚本
- **引擎类**（决定推理能力的底层）→ 选好 LLM，别在应用层硬改它
- **知识类**（LLM 答不准 / 知识过期）→ 上 RAG，把知识库检索结果注入上下文
- **治理类**（要控权限、留证据、可暂停回滚）→ 交给 Harness，别在业务层裸奔

---

**文档版本**: v1.3
**最后更新**: 2026年07月
**整理人**: 王新骏
